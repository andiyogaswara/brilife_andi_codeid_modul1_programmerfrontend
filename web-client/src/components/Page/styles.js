const styles = theme => ({
    root: {
        display: 'flex,'
    },
    // toolbar: theme.mixins.toolbar,
    content: {
        flexGrow: 1,
        padding: theme.spacing(3),
        marginLeft: 220,
        marginTop: 20
    }
});

export default styles;